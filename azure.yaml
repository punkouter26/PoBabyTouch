# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: PoBabyTouch
metadata:
  template: PoBabyTouch@0.0.1-beta

# Infra folder for infrastructure definition
infra:
  provider: bicep
  path: infra

# Services to deploy
services:
  api:
    project: ./Server/PoBabyTouchGc.Server
    language: dotnet
    host: appservice

# Hooks to run commands at different stages
hooks:
  preprovision:
    windows:
      shell: pwsh
      run: |
        Write-Host "🚀 Starting provisioning for PoBabyTouch..." -ForegroundColor Green
    posix:
      shell: sh
      run: |
        echo "🚀 Starting provisioning for PoBabyTouch..."
  
  postprovision:
    windows:
      shell: pwsh
      run: |
        Write-Host "✅ Infrastructure provisioned successfully!" -ForegroundColor Green
    posix:
      shell: sh
      run: |
        echo "✅ Infrastructure provisioned successfully!"